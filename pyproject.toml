[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "PoreFinding"
authors = [{name = "David Seiferth", email = "david.seiferth@oriel.ox.ac.uk"}]
dynamic = ["version", "description", "dependencies", "classifiers"]
